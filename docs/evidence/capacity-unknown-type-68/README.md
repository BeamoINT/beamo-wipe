# Capacity wording and unknown type

Large disks show a familiar decimal TB name plus the exact GB used as the
confirm token. Unknown media kind is labeled Type unknown instead of a blank
chip. Confirm tokens, boot-USB exclusion, and nwipe flags are unchanged.

Fake lsblk / DryRunRunner only.
